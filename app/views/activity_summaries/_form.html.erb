<%= form_for(@activity_summary) do |f| %>
  <% if @activity_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_summary.errors.count, "error") %> prohibited this activity_summary from being saved:</h2>

      <ul>
      <% @activity_summary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_id %><br>
    <%= f.collection_select(:itype_id, @itypes, :id, :name, {:include_blank => "No type"}) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :qualifier %><br>
    <%= f.text_field :qualifier %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
