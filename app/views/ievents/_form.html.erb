<%= form_for(@ievent) do |f| %>
  <% if @ievent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ievent.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @ievent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- div class="field">
    <%= f.label :activity_id %><br>
    <%= f.number_field :activity_id %>
  </div -->
  <!-- div class="field">
    <%= f.label :type_id %><br>
    <%= f.collection_select(:itype_id, @itypes, :id, :name, {:include_blank => "No type"}) %>
  </div -->
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, size: 50 %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
