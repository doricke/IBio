<%= form_for(@data_sync) do |f| %>
  <% if @data_sync.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_sync.errors.count, "error") %> prohibited this data_sync from being saved:</h2>

      <ul>
      <% @data_sync.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :individual_id %><br>
    <%= f.number_field :individual_id %>
  </div>
  <div class="field">
    <%= f.label :instrument_id %><br>
    <%= f.number_field :instrument_id %>
  </div>
  <div class="field">
    <%= f.label :algorithm_id %><br>
    <%= f.number_field :algorithm_id %>
  </div>
  <div class="field">
    <%= f.label :updated_at %><br>
    <%= f.datetime_select :updated_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
