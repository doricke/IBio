<%= form_for(@gene_reaction) do |f| %>
  <% if @gene_reaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gene_reaction.errors.count, "error") %> prohibited this gene_reaction from being saved:</h2>

      <ul>
      <% @gene_reaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pathway_id %><br>
    <%= f.number_field :pathway_id %>
  </div>
  <div class="field">
    <%= f.label :reaction_id %><br>
    <%= f.number_field :reaction_id %>
  </div>
  <div class="field">
    <%= f.label :gene_id %><br>
    <%= f.number_field :gene_id %>
  </div>
  <div class="field">
    <%= f.label :role_itype_id %><br>
    <%= f.number_field :role_itype_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
